# __SITE_NAME__

Generated by Neway Commerce OS.

## Included

- React + Vite storefront with Tailwind CSS 3
- 7-page routing: Home, Shop, Product Detail, Cart, Checkout, Wishlist, Admin
- Zustand cart + wishlist stores with `persist` middleware
- AI shopping concierge widget (keyword-based mock responses)
- Slide-out CartDrawer with quantity controls
- Three-step Checkout (shipping → payment → review) with progress stepper
- ProductCard with grid/list dual layout, hover image swap, quick-add
- Responsive design with mobile menu
- EdgeOne Pages deployment config

## Local development

```bash
npm install
npm run dev
```

## Build for production

```bash
npm run build
```

The `dist/` folder is ready to deploy to EdgeOne Pages or any static host.

## Notes

This project is scaffolded for fast iteration.
- Mock product data is in `src/data/products.ts`
- AI concierge uses keyword-based mock responses in `AIChatWidget.tsx`
- Payment flow uses mock checkout (no real Stripe integration)
- Some backend handlers are placeholders and should be connected to real services for production use
